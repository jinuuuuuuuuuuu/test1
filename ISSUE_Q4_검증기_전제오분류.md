# 문제 상황: ④검증기가 답변 결함을 "질문의 잘못된 전제"로 오분류해 ⑤가 답변을 회피함

> **상태: 해결됨 (2026-09-02).** 4개 계층 수정 + 회귀 테스트 5건 추가.
> 이전 제목은 `ISSUE_Q4_생성기_초안무시.md`였다 — 최초 진단이 원인을 ⑤ 생성기로
> 지목했으나, 실행 로그 대조 결과 **원인은 ④ 검증기의 `premise_issues` 오분류**로
> 확정돼 파일명을 바꿨다. 정정 내역은 아래 "최초 진단의 오류" 절에 남긴다.

## 재현 방법

주최측 참고용 질의 5개 중 Q-4(상품 비교, Open).

```
질문: "솔로몬 국공채 단기·중장기·장기, 뭐가 달라요? 안정적인 걸 원해요."
```

확정 근거는 배치 실행 로그다: `eval/results/eval_run.jsonl` **199번째 줄**(`no: 199`,
대주제 "상품설명·비교", 중점평가지표 "근거완전성,근거기반,요구사항충족", 2026-09-02 03:21 실행).

## 증상

③ 상품 Agent가 솔로몬 국공채 펀드를 전 클래스 조회해 놓고도, 최종 답변 첫 문장이 회피로 시작한다:

> 솔로몬 국공채 단기, 중장기, 장기 펀드의 구체적인 차이점과 특징에 대한 정보는
> **제공된 자료에서 찾을 수 없으므로**, 이에 대해 정확히 알려드릴 수 없습니다.

그런데 같은 답변의 `참고 근거:` 줄에는 솔로몬 펀드 8종이 실명으로 올라와 있다
(초단기 2호 C1/C-e, 단기 1호 C-P/C-P2, 중장기 1호 C/C-e, 장기 1호 C/C-e).
**근거를 손에 쥔 채 "자료에 없다"고 답한 것이다.**

## 로그에 기록된 실제 verification

```json
"grounded": true,
"issues": [],
"unsupported_numbers_confirmed": [],
"requirements_met": false,
"missing_requirements": ["솔로몬 국공채 단기·중장기·장기 펀드의 구체적인 차이점과 특징"],
"premise_issues":       ["솔로몬 국공채 단기·중장기·장기 펀드의 구체적인 차이점과 특징"]
```

`flow`: `repair_attempted: true`, `needs_clarification: false`, `recommendation_stage: null`,
`response_mode: "complete"`. `router.intent`: `["정보형", "상품형"]`.

**핵심은 마지막 두 줄이다 — ④가 완전히 동일한 문자열을 `missing_requirements`와
`premise_issues` 양쪽에 넣었다.** 그리고 `grounded`는 `true`였다(근거 검증은 전부 통과).

## 근본 원인: `premise_issues` 오염 → ⑤ 프롬프트 지시 발동

원인은 세 단계로 이어진다.

### 1) ④가 답변 결함을 "질문의 잘못된 전제"로 오분류

`premise_issues`는 정의상 **사용자가 질문에서 말한 내용 중 사실과 다른 것**만 담아야 한다
("세금 감면이 어마어마하다던데" 같은 유도성 전제). 그런데 ④가 여기에 넣은 것은
"초안이 펀드 차이를 안 다뤘다"는 **답변 결함 서술**이다. 사용자가 요구한 것이 곧
사용자의 틀린 전제일 수는 없으므로, 이 중복은 정의상 성립하지 않는다.

### 2) 기존 방어 함수 `split_premise_issues`가 이 항목을 못 걸러냄

`verification.py`에는 이미 이 오분류를 잡는 `is_answer_defect_statement`가 있다. 그러나
이 함수는 `_ANSWER_DEFECT_MARKERS`("~다루지 않", "누락", "부족" 등) 문구 매칭에 의존하는데,
문제의 항목은 **동사 없는 순수 명사구**("~펀드의 구체적인 차이점과 특징")라 마커에 하나도
걸리지 않는다. 실제로 확인한 결과:

```
is_answer_defect_statement(item) -> False   # 진짜 전제로 통과
is_benign_condition_statement(item) -> False
```

마커 목록에 명사구 패턴을 더 넣는 방식으로는 막을 수 없다 — 결함 항목은 어떤 명사구로도
표현될 수 있어 블랙리스트가 계속 뚫린다.

### 3) ⑤ 프롬프트가 오염된 `premise_issues`를 받아 "전제를 바로잡아라" 지시를 실행

`GENERATOR_SYSTEM_PROMPT`의 지시:

> premise_issues(질문의 잘못된/과장된 전제)가 있으면, **답변 시작 부분에서** 그 전제를
> 짚고 바로잡은 뒤 본 답변으로 넘어가세요

LLM은 이 지시를 정확히 따랐다 — "펀드의 차이점과 특징"을 바로잡아야 할 전제로 받고,
답변 첫 문장을 "그 정보는 자료에서 찾을 수 없다"로 열었다. **⑤의 과잉 해석이 아니라
오염된 입력에 대한 정상 동작이다.**

`generator.py`에 정제 로직(`split_premise_issues` + 중복 제거)이 있긴 했으나
**LLM 응답을 받은 뒤에 실행**됐다. 사후 정제는 출력에 붙는 교정문만 없앨 뿐,
이미 회피형으로 쓰인 본문은 되돌리지 못한다.

### 왜 코드 강제 계층이 통과시켰나

`enforce_missing_requirements`는 `has_limit_disclosure(answer)`가 True면 즉시 반환한다
(중복 고지 방지). 회피 답변의 "제공된 자료에서 찾을 수 없으므로"가 정확히 이 마커에
걸려서, **강제 계층 전체가 무동작으로 통과**했다. 확인: `has_limit_disclosure(answer) -> True`.

## 수정 내역 (4계층)

| # | 파일·위치 | 수정 |
|---|---|---|
| 1 | `verification.py` `split_premise_issues` | `missing_requirements`를 선택 인자로 받아, **양쪽에 중복 기재된 항목을 무조건 misfiled로 분류**. 마커 판정보다 강한 구조적 신호라 명사구도 잡힌다. `_compact_for_duplicate`로 공백·서식·구두점 차이를 흡수 |
| 2 | `generator.py` 프롬프트 조립부 | `split_premise_issues`를 **LLM 호출 전에** 적용해 프롬프트 오염 차단. `prompt_premise_issues`/`prompt_missing`을 프롬프트에 사용 |
| 3 | `grounding.py` `GROUNDING_SYSTEM_PROMPT` 3번 항목 | (a) "질문이 특정 상품명을 지목하면 그 상품 데이터가 곧 정답" 원칙 추가 + 1번 "근거 적격성"과의 차이 명시(좁은 질문에는 좁은 근거가 맞다), (b) "같은 내용을 두 필드에 동시 기재 금지" 명시 |
| 4 | `verification.py` `apply_requirement_scope_override` | `evidence_sources` 인자 추가. 질문이 지목한 고유명사가 **근거 출처명에 실재할 때만** 해당 누락 판정을 제거(`_question_names_specific_products`). ③이 조회 못 한 경우는 그대로 누락 유지 |

`grounding.py`에서 `apply_requirement_scope_override` 호출부에 `evidence_sources=[c["source"] for c in context]`를 전달하도록 연결.

### 수정 후 검증 (로그의 실제 verification으로 재현)

```
BEFORE  premise_issues: [솔로몬...차이점과 특징]  missing: [동일]  requirements_met: False
AFTER   premise_issues: []                        missing: []      requirements_met: True
```

`requirements_met`이 True로 돌아오므로 **repair loop 자체가 발동하지 않는다**
(`graph.py:_route_after_grounding`).

### 오탐 점검

특정 상품 지목 판정이 일반 질문에 잘못 발동하지 않는지 확인:

```
"연금저축 펀드 환매 제한이 있나요?"        -> []          (제도 일반 질문, 미발동)
"펀드가 뭐예요?"                          -> []
"안정적인 상품 추천해줘"                   -> []
"솔로몬 국공채 단기·중장기·장기, 뭐가 달라요?" -> ['솔로몬']  (발동)
"TDF2045 어때요?"                         -> ['TDF2045']  (발동)
```

첫 번째 케이스는 ④ 프롬프트 1번 "근거 적격성" 규칙(특정 펀드 근거로 제도 전체를 단정하지
말 것)이 지키는 시나리오와 정확히 같다 — 미발동이 확인돼 **두 규칙이 충돌하지 않는다.**

진짜 전제(`"세금 감면이 어마어마하다던데"`)는 중복이 아닌 한 그대로 유지된다.

### 테스트

`tests/test_agents/test_verification.py`에 회귀 5건 추가.
전체 스위트: **543 passed, 19 skipped** (수정 전 538 passed와 동일 기준, 실패 0).

## 최초 진단의 오류 (기록용)

최초 분석은 디버그 재실행 결과를 근거로 작성돼, 배치 로그(`eval_run.jsonl`)와 대조하니
세 가지가 사실과 달랐다. 같은 실수를 반복하지 않기 위해 남긴다.

1. **`missing_requirements` 원문이 달랐다.** 최초 진단은
   `"질문은 ... 일반적인 설명을 요구하고 있으나, 초안은 특정 펀드들에 대한 정보만을
   제공하고 있어..."`라는 긴 판정 서술문을 인용했으나, 로그의 실제 값은 명사구 한 줄이다.
   이 차이가 결정적이다 — 긴 서술문이었다면 `_requirement_already_covered`의
   `"질문은"/"있으나"` 마커에 걸리는 별개 경로가 문제였겠지만, 실제 명사구는
   그 마커에 안 걸리고 **숫자 토큰이 없어서** 필터를 통과한다.
2. **`premise_issues` 오염이 진단에서 완전히 누락됐다.** 최초 진단은 원인을
   `missing_requirements` 단독으로 봤으나, 실제 트리거는 `premise_issues` 쪽이다
   (⑤ 프롬프트의 "답변 시작 부분에서 전제를 바로잡아라" 지시).
3. **`grounded`는 `false`가 아니라 `true`였다.** 근거 검증은 전부 통과했고
   `unsupported_numbers_confirmed`도 빈 배열이다. 최초 진단이 언급한
   `["4.00%", "3.10%"]` 수치 오차는 이 로그에 존재하지 않는다(다른 실행의 결과로 보인다).

또한 최초 진단은 "써야 하는데 안 쓴 것을 막는 대칭 강제 장치가 없다"를 구조적 공백으로
지목했으나, `apply_requirement_scope_override`가 정확히 그 역할(④의 잘못된 누락 판정을
삭제하고 `requirements_met`을 되돌림)을 이미 하고 있었다. 실제 공백은 그 함수의 판정
헬퍼가 **중도인출·전세대출 문맥 전용 하드코딩**이라 상품 비교 질문에 해당 규칙이
없었던 것이다 — 그래서 수정 #4를 새 함수가 아니라 그 함수 안에 넣었다.

## 남은 과제

- **간헐성**: LLM 비결정성 탓에 ④ 판정은 실행마다 달라진다. 수정 #1·#2·#4는 코드 강제라
  판정이 어떻게 나오든 방어되지만, #3(프롬프트)은 확률적이다.
- **전수 스캔 미착수**: 501문항 중 `premise_issues`와 `missing_requirements`에 동일
  문자열이 들어간 케이스가 더 있는지 확인하면 이 결함의 실제 발생 빈도를 알 수 있다.
  수정 #1이 전부 흡수하므로 재실행 없이 로그 스캔만으로 가늠 가능하다.
- **본문 삭제 감지(L0류 사후 검사)는 만들지 않았다.** 최초 진단이 제안한 방향이지만,
  원인이 프롬프트 오염으로 특정돼 #1·#2가 경로를 직접 차단한다. 정상적인 축약과
  회피를 구분하기 어려워 오탐 위험이 크므로 보류한다.

## 별도 세션 재검증 (2026-09-02, 커밋 전 워킹트리 기준)

수정된 코드로 동일 질문("솔로몬 국공채 단기·중장기·장기...")을 실제 그래프
(`build_graph().invoke(...)`)로 3회 재실행.

- **회피 문구("제공해 드릴 수 없습니다") 0/3** — 핵심 결함 해소 확인.
- 3회 전부 위험등급·보수율·1년/3년 수익률·설정액까지 정확한 개별 수치로 답변.
- 전체 테스트 스위트 543 passed, 19 skipped, 실패 0(재확인).

**부수 발견(새 오탐, 이 이슈의 범위 밖으로 별도 기록)**: 1회는 `grounded=False`가
떴는데, 원인은 이 이슈와 무관한 별개 오탐이었다 — "안정적인 걸 원해요"(사용자의
**선호 진술**)를 `premise_issues`("사실과 다른 전제")로 오판해 "먼저 질문에 담긴
전제를 짚고 넘어가겠습니다..." 도입부가 붙었다. 다만 답변 본문의 수치 자체는
정확했고 회피는 없었다 — 이번 수정으로 막은 "근거를 손에 쥐고도 거부"와는 다른
성격의 경미한 결함이다. 추가 조사 필요: `is_benign_condition_statement` 또는
`_premise_is_pension_context_false_negative`류 판정에 "사용자의 위험선향/선호
표현"(예: "안정적인 걸 원해요", "공격적으로 투자하고 싶어요")을 전제 오류에서
제외하는 규칙이 빠져 있을 가능성.
