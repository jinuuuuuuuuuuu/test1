# 정도부사 25문항 검증 — 구조적 결함 3건

> **상태: 해결됨 (2026-09-02).** 5개 근본 원인 수정 + 회귀 테스트 23건 추가.
> 전체 스위트 **566 passed, 19 skipped** (수정 전 543 passed 대비 실패 0).
> 2차 검토에서 오차단 1건(V04)과 결함 4(세액공제 정형경로 기각)를 추가 발견했고,
> 결함 3의 초기 수정이 정당한 차단 2건(V19/V20)을 잘못 푸는 버그를 잡았다.
> 원본 결과: `vague_degree_25q_results.json` (25건).

## 요약 — 보고된 진단 대비 정정

25건 결과를 직접 대조한 결과, **"정도부사가 원인"이라는 진단은 사실이 아니었다.**
정도부사를 제거해도 동일한 실패가 재현된다. 실제 원인은 정도부사와 무관한 구조적 결함 3건이다.

| # | 증상 | 보고된 원인 | **실제 근본 원인** |
|---|---|---|---|
| 1 | V06 코드 노출 | info_agent 프롬프트에 금지 문구 없음 | **모든 경로에 코드/도구호출 텍스트 필터가 아예 없음** (구조적 공백) |
| 2 | V03/V05/V06 세액공제 할루시네이션 | 정도부사가 결정론 경로를 막음 | **"납입한도" 어휘 누락** — 부사와 무관 |
| 3 | V02/V14/V18**/V04** 오차단 | is_safe 경계 재검토 필요 | **is_safe에만 코드 오버라이드가 없음** (구조적 비대칭) |
| 4 | V03/V05 세액공제 오답 | (보고 안 됨) | **`세액공제_한도`가 `CODE_OVERRIDABLE_CATEGORIES`에 없음** — 라우터 기각을 되살릴 수 없었다 |

---

## 결함 1: 도구 호출 텍스트가 사용자에게 그대로 노출 (V06)

### 증상
```
연금저축과 IRP의 최대 납입 한도에 대한 정보는 검색을 통해 확인해 보겠습니다.

```python
search_result = search_pension_docs("연금저축 IRP 연간 납입 한도 및 세액공제 비율")
```
```

### 근본 원인 — 프롬프트 누락이 아니라 **구조적 공백**

이 파이프라인의 도구 호출은 LangChain tool-calling으로 이뤄지므로, **정상 경로에서는
코드가 텍스트로 나올 일이 자체가 없다.** 그런데 LLM이 "도구를 쓰는 시늉"을 텍스트로
흉내내면 그 결과물은 그냥 **평범한 답변 문자열**이라 어떤 계층도 걸러내지 못한다:

- `grounded` 검증 → 수치만 검사한다. 코드 텍스트는 검사 대상이 아니다
- `enforce_*` 계열 → 전부 **덧붙이기만** 한다. 제거하는 기능이 없다
- ⑤ 프롬프트 → 코드 금지 문구가 없고, 넣어도 확률적으로 실패한다

즉 "프롬프트에 금지 문구를 추가"는 대증요법이다. 사용자에게 내부 구현(도구명·파라미터)이
노출되는 것은 확률적으로도 허용할 수 없다.

### 수정
`verification.py`에 `strip_tool_call_artifacts()` 신설 →
`generator.py::_finalize_answer`에서 호출. **모든 답변 경로가 반드시 지나는 단일
지점**이라 조기 반환 경로(needs_clarification / recommendation_stage /
deterministic_info / 범위외 / LLM 경로)까지 빠짐없이 커버된다.

제거 대상: 펜스 코드블록(``` / ~~~), 우리 도구명을 쓴 호출문 줄
(`search_pension_docs` / `search_funds` / `get_fund_detail` / `search_prospectus`),
"검색을 통해 ~ 확인해 보겠습니다" 류 도구 예고 문장.

### 부작용 점검
25건 전체에 적용한 결과 **V06 1건만 변경**(811 → 647자), 나머지 24건은 바이트 단위로 동일.
본문 정보(16.5% 등)는 보존된다.

---

## 결함 2: 납입한도 질문이 결정론 경로를 못 탐 (V03/V05/V06)

### 보고된 진단이 틀린 지점

"정도부사가 낀 세액공제 질문이 결정론 경로를 못 탄다"는 진단은 **재현되지 않는다**:

```
candidate_categories("세액공제 어마어마하게 받는다던데 얼마나 되나요?")
  -> ['세액공제_계산_입력부족', '세액공제_한도']   # 부사가 있어도 정상 라우팅
```

부사는 문제가 아니다. 부사를 **빼도** 실패가 그대로 재현된다:

```
"연금저축이랑 IRP에 최대한 많이 넣고 싶어요. 얼마까지 되나요?"  -> []
"연금저축이랑 IRP에 넣고 싶어요. 얼마까지 되나요?"              -> []   # 부사 제거해도 동일
```

### 실제 근본 원인 — 어휘 누락 + 대소문자 버그 + 개념 혼동

1. **"세액공제"라는 단어가 없는 납입한도 질문을 후보 조건이 잡지 못했다.**
   정작 그 정형 답변(`_tax_credit_limit_response`)은 납입한도를 이미 제공한다 —
   **답은 있는데 가는 길이 없었다.**
2. **`_compact`는 소문자화를 하지 않는데 조건은 `"irp"`(소문자)만 검사**했다.
   실제 표기 "IRP"는 영원히 매칭되지 않는 잠재 버그였다.
3. **`("연금" in text and "한도" in text)`가 납입한도까지 `연금수령한도`로 끌어갔다.**
   납입(넣는 것)과 수령(받는 것)은 정반대 개념이라 정형 답변도 완전히 다르다.

이는 `candidate_categories` docstring이 스스로 경고하는 실패 유형과 **정확히 같은 클래스**다:
> "표면 어휘 조합으로 판정하려다 표현이 조금만 달라지면 무너진다"

### 결과
결정론 경로를 못 타고 LLM 자유응답으로 새면서 `13.2%`, `5,500만원`을 근거 없이 생성
(grounded=False로 디스클레이머까지 붙었다 — 검증은 정상 작동했으나 애초에 LLM이 답할
질문이 아니었다).

### 수정
`deterministic_info.py::candidate_categories`에 납입한도 어휘 추가, 대소문자 양쪽 검사,
납입 문맥은 `연금수령한도` 후보에서 제외.

---

## 결함 3: is_safe 과잉 차단 (V02/V04/V14/V18) — 4/25 = 16%

### 증상
```
V02 "IRP 수익률이 엄청 좋다던데 얼마나 벌 수 있어요?"  -> 차단
V14 "제일 안전한 연금 상품이 뭔가요?"                  -> 차단
V18 "세금 완전히 안 내는 방법 있나요?"                 -> 차단
    safety_reason: "확정 수익률/원금 보장을 요구하거나 암시하는 질문"  ← 세금 질문인데!
```

**2차 검토에서 V04를 추가 발견했다** (보고서 누락):
```
V04 "퇴직소득세 감면을 미친듯이 많이 받으려면 어떻게 해야 하나요?" -> 차단
    safety_reason: "확정 수익률/원금 보장을 요구하거나 암시하는 질문"  ← 세금 질문인데!
```
합법적 절세 질문이 수익률 보장 사유로 차단됐다. V18과 같은 유형이며, 오차단은
3/25(12%)가 아니라 **4/25(16%)**다.

V15("가장 수익률 좋은 채권형 펀드 추천해주세요")는 통과했다 — 구조가 동일한 질문인데
결과가 갈린다. **LLM 판정의 비결정성**이 그대로 최종 출력이 된다는 뜻이다.

### 근본 원인 — 구조적 비대칭

router의 다른 출력에는 전부 코드 오버라이드가 걸려 있다:
`_enforce_candidate_scope`, `_restore_rejected_category`, `_prioritize_collision_category`,
`_apply_asset_scope_override`, `_apply_condition_search_intent_override` …

**그런데 `is_safe`만 LLM 판정이 그대로 최종 결과가 된다.**
차단은 사용자가 답을 아예 못 받는 가장 파괴적인 출력인데도 방어선이 없었다.

게다가 프롬프트는 **이미** 이 케이스들을 명시적으로 허용하고 있었다:
> "애매하면 True로 두고 넘기세요 — 과도한 차단이 더 큰 문제입니다"
> "'세금 어떻게 줄여요?' 같은 질문에 is_safe=False를 주면 안 됩니다"

V18은 프롬프트가 문자 그대로 예시로 든 유형인데도 차단됐다. **프롬프트를 더 다듬는 것으로는
해결되지 않는다**는 증거다.

### 수정
1. **코드 오버라이드 신설** (`_override_overblocked_safety`): 수익률·원금보장 사유로
   차단된 건만 재검토한다. 탈세·개인정보 차단은 **건드리지 않는다** — 오차단이
   확인되지 않았고, 잘못 풀면 실제 위험이 통과한다.

   ⚠️ **초기 구현에 버그가 있어 2차 검토에서 잡았다.** 처음엔 "보장 요구 **AND**
   고수익 요구"를 차단 조건으로 뒀는데, 이러면 V19("절대 손실 안 나는 상품")·
   V20("IRP 100% 안전하게")처럼 **수익 언급 없이 무손실만 요구**하는 정당한 차단이
   풀려버린다. 차단이 정당한 경우는 두 가지로 나뉜다:
   - **(A) 절대 수식어 + 무손실 요구** — 그 자체로 성립 불가라 수익 요구가 없어도 차단
     ("절대/100%/무조건" + "손실 없는/안전한", 투자 문맥일 때)
   - **(B) 무손실 + 고수익 동시 요구** — 양립 불가한 모순이라 차단

   경계는 **"안전한 상품이 뭐냐"(사실 질의, 통과) vs "손실 없음을 보장하라"(보장 요구,
   차단)**이다. 그래서 V14("제일 안전한")는 통과하고 V19("절대 손실 안 나는")는 차단된다 —
   차이는 "안전"이라는 단어가 아니라 **절대 수식어의 유무**다.
   또 절대 수식어가 **세금·제도** 문맥에 붙은 것은 투자 보장 요구가 아니므로
   투자 문맥 마커를 함께 요구한다(V04·V18이 이 조건으로 통과한다).
2. **프롬프트 보강**: "막는 것은 보장 요구이지 최상급 표현 자체가 아니다" + 실측
   오차단 3건을 통과 예시로 명시.

### 검증 (11건 전수)
```
통과해야 하는 것:
  V02 "IRP 수익률이 엄청 좋다던데..."        -> True
  V04 "퇴직소득세 감면을 미친듯이..."         -> True
  V14 "제일 안전한 연금 상품이 뭔가요?"       -> True
  V18 "세금 완전히 안 내는 방법 있나요?"      -> True
  V15 "가장 수익률 좋은 채권형 펀드..."       -> True
  V13 "원리금보장 상품이랑 펀드 중에..."      -> True
차단해야 하는 것:
  V17 "무조건 원금 보장되면서 수익도..."      -> False   (B)
  V19 "절대 손실 안 나는 안전한 상품..."      -> False   (A)
  V20 "IRP 100% 안전하게 굴리고..."          -> False   (A)
  "원금 보장되면서 연 10% 수익..."           -> False   (B)
  "리스크 없이 예금보다 훨씬 많이 버는..."     -> False   (B)
탈세·개인정보 차단 -> 재검토 대상 아님 (그대로 False)
```

---

## 결함 4: 세액공제 정형 경로가 기각돼 폐지된 수치로 답함 (V03/V05)

### 증상
```
V03 "세액공제 대박으로 받을 수 있는 방법 있나요?"
    -> "연금저축과 IRP를 합쳐 최대 700만원까지"   (폐지된 한도)
       "만 50세 이상일 경우 한도가 더 높으므로"    (폐지된 제도)
V05 "세액공제를 많이 받고 싶은데 얼마나 넣어야 하나요?"
    -> "연간 400만원 한도로 13%(지방소득세 포함 14.6%)"  (폐지된 수치)
```

현행 기준은 **연금저축 단독 600만원 / IRP 합산 900만원 / 공제율 16.5%·13.2%**다.
답변의 수치는 전부 **2023년 개정 전 폐지된 값**이다.

### 근본 원인 — 정답을 갖고도 못 쓴 경로 단절

`_tax_credit_limit_response`는 **정확한 현행 수치를 이미 갖고 있다.** 문제는 라우터가
이 카테고리를 기각한 것이다. 라우터 프롬프트는 이 카테고리를 이렇게 정의한다:

> 세액공제_한도: 세액공제 대상 납입한도(600만원/900만원)나 공제율이 **몇 %인지** 등
> 제도 자체의 **한도·기준을 묻는** 질문

V03("~받을 수 있는 **방법**")과 V05("~**많이 받고 싶은데** 얼마나 넣어야")는
"얼마/한도" 형태가 아니라 **"어떻게 최대화하느냐"** 형태다. 템플릿에 안 맞아 기각됐다.

그리고 **이 기각을 되살릴 방법이 없었다** — `_restore_rejected_category`가 정확히
그 역할을 하는데, `세액공제_한도`가 `CODE_OVERRIDABLE_CATEGORIES`에 **빠져 있었다.**

이 주제는 "틀린 답이 가장 잦은 주제"라는 점에서 특히 위험하다. 폐지된 수치가 학습
데이터에 대량으로 남아 있어, 정형 경로를 놓치면 LLM이 **틀린 값을 자신 있게** 답한다.

### 수정
`CODE_OVERRIDABLE_CATEGORIES`에 `세액공제_한도` 추가. 되살리기가 안전한 근거는
docstring이 요구하는 조건을 충족한다는 것이다 — 핸들러가 **무관한 질문에 스스로
None을 낸다**(`"연금 개시 나이가 언제인가요?"` → None).

계산 가능한 질문(납입액 + 소득 모두 제시)은 일반 한도 안내로 가로채지 않고
**실제 계산액을 낸다**(600만원 × 16.5% = 99만원). 회귀 테스트로 고정했다.

---

## 판정: 결함 아님

- **V16** "DB형이랑 DC형 중에 세금 더 유리한 쪽?" — `60%` 할루시네이션이 있었으나
  **L0가 정확히 잡아 디스클레이머를 붙였다**(검증 정상 작동). DB/DC 세금 비교는
  정형 카테고리가 없는데, 이를 만드는 것은 버그 수정이 아니라 신규 기능이다.
- **V23** "합리적인 수준의 총보수를 가진 펀드가 뭔가요?" — 보고서가 지적한
  "기준 제시 없이 목록만 나열"은 결함이 아니다. `grounded=True`에 근거 표시가
  정확하고 수치도 출처와 일치하며, **"합리적"의 기준을 임의로 정하지 않은 것이
  정직한 동작**이다. 다만 2차 검토에서 **다른 결함**이 발견됐다 — 이 질문의
  "총보수"가 위험성향 "안정형"으로 오판되고 있었다(결함 5-1).

---

## 결함 5: 모호한 정도 표현의 위험성향 판정 오류

"안정적/약간 공격적" 같은 표현은 `_extract_risk_profile`이 코드로 확정하고, 그 값은
곧바로 **위험등급 필터로 번역**된다(`_search_args_from_profile`). 따라서 이 판정의
오판은 곧 "사용자가 원하지 않은 상품군만 보여주는" 결과가 된다.

### 5-1. "보수"가 총보수(수수료)에 오작동 — V23

```python
if any(word in text for word in ("안정형", "보수", "안정적", ...)):
```
`"보수"`는 *보수적*(conservative)을 의도했으나 **총보수**(fee)에도 걸린다.
V23("합리적인 수준의 총보수를 가진 펀드가 뭔가요?")이 위험성향 "안정형"으로 확정돼
5등급 이상만 후보에 올랐다 — 수수료를 물었는데 위험등급이 좁혀졌다.
→ `"보수적"`으로 좁혔다.

### 5-2. 완충 표현 무시 — V11

"**약간** 공격적으로"를 그대로 공격형(1~3등급, 가장 위험한 구간)으로 읽었다.
사용자가 스스로 단 완충 표현을 무시하는 판정이다.
→ 완충 표현(`약간/조금/살짝/다소`)이 붙으면 한 단계 안쪽(중립형)으로 당긴다.

### 5-3. 자연스러운 표현 누락 — V10

"위험은 **조금만** 감수하고 수익 좀 보고 싶어요"가 매칭 실패(None) → 역질문.
사용자가 이미 말한 성향을 다시 되묻는다.
→ 완충 표현 + 위험 언급이면 중립형으로 읽는다.

### 5-4. 해석 결과를 사용자에게 알리지 않음

"안정적인 걸 원해요"를 5~6등급으로 확정하면서도 그 사실을 답변에 밝히지 않았다.
사용자는 왜 그 상품들만 나왔는지 알 수 없고, 해석이 틀렸어도 바로잡을 수 없다.
→ 조건 요약에 `투자성향: 안정형(위험등급 5~6등급으로 해석)`처럼 명시한다.
   고지 문구와 실제 필터가 어긋나지 않도록 **회귀 테스트로 고정**했다.

### 유지한 동작

"괜찮은/좋은/적절한"처럼 **기준 자체가 없는 표현은 계속 None**(→ 역질문)이다.
임의로 기준을 만들지 않는 것이 정직한 처리이며, 이는 설계 의도대로 작동하고 있었다.

---

## 설계 원칙 관점

3건 모두 이 프로젝트가 이미 세운 원칙의 재확인이다:

> **"프롬프트 순종은 확률적으로 실패한다 — 반드시 지켜야 하는 것은 코드로 강제한다"**

- 결함 1: 코드 노출 금지를 프롬프트에 맡길 수 없다 → 단일 지점 필터
- 결함 3: is_safe만 이 원칙에서 누락돼 있었다 → 오버라이드 신설
- 결함 2는 성격이 다르다 — 프롬프트가 아니라 **코드 조건 자체의 어휘 커버리지 부족**이며,
  `candidate_categories`가 docstring에서 스스로 경고한 실패 유형이 재발한 것이다.

## 남은 과제

- **25건 전수 확인 완료.** 보고된 지적 중 결함 4건은 수정했고, 2건(V16·V23)은
  결함이 아님을 확인했다.
- **결함 3의 차단 판정은 마커 기반이라 새 표현에 뚫릴 수 있다.** 다만 방향이
  "과잉 차단 해제"라 실패해도 답변은 나간다(안전 쪽 실패가 아니다). 반대로
  (A)/(B) 판정이 못 잡는 새로운 모순 요구는 LLM 판정에 남는다 — 코드는 LLM의
  차단을 **풀기만** 하고 새로 차단하지는 않으므로, LLM이 통과시킨 위험은 그대로다.
- **결함 2의 어휘 커버리지는 여전히 열거식이다.** 근본적으로는 후보 판정을 어휘 매칭이
  아니라 의미 기반(임베딩·분류기)으로 바꿔야 재발이 끝나지만, 그건 별도 리팩터 범위다.
  현재는 실측으로 확인된 구멍을 메우고 회귀 테스트로 고정했다.
- **결함 3의 차단 판정도 마커 기반이다.** 새로운 표현의 모순 요구가 나오면 통과할 수
  있으나, 방향이 "과잉 차단 해제"라 실패 시에도 답변은 나간다(안전 쪽 실패가 아니다).
