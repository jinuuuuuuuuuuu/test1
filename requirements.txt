# --- LLM / Agent orchestration ---
# 버전을 고정한다 — ChatClovaX가 langchain_openai.BaseChatOpenAI를 상속하는데,
# with_structured_output()의 기본 method가 버전에 따라 달라질 수 있어(현재는 "function_calling"),
# 팀원 환경마다 다른 버전이 깔리면 동작이 미묘하게 달라질 위험이 있다.
langgraph==1.2.11
langchain==1.3.15
langchain-core==1.5.4
langchain-openai==1.4.3
langchain-naver==0.1.1   # ChatClovaX (HyperCLOVA X). CLOVASTUDIO_API_KEY 환경변수 필요 (.env.sample 참고)

# --- API server ---
fastapi
uvicorn[standard]
pydantic==2.13.4
python-dotenv

# --- Document parsing (Phase 1) ---
pymupdf         # PDF
python-docx     # DOCX
python-pptx     # PPTX
openpyxl        # XLSX

# --- Storage ---
chromadb        # 벡터DB (docs.zip 서술형 문서 색인)
langchain-chroma  # LangChain <-> Chroma 연동
langchain-text-splitters  # 임베딩 API 컨텍스트 길이 제한 대응 긴 청크 분할
sqlalchemy      # 구조화 DB (투자설명서 6축, doc29/doc34 lookup)

# --- Data handling ---
pandas
numpy

# --- Testing ---
pytest
